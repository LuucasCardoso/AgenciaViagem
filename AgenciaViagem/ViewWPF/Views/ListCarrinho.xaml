<UserControl x:Class="ViewWPF.Views.ListCarrinho"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewWPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Height="auto" Width="auto">
    <Grid x:Name="GridPrincipal" Width="608" Height="443">
        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect SlideInFromLeft, Duration=0:0:0.8}" Grid.ColumnSpan="2">
        <Grid x:Name="GridSuperior" Width="580" Height="65" Margin="18,6,10,371">
            <Button Width="250" Height="50" Margin="0,5,330,10" Background="LightGreen" Content="Comprar Mais" Foreground="Black" Click="ComprarMais_Click"/>
            <Button Width="250" Height="50" Margin="330,5,0,10" Background="Yellow" Content="Atualizar" Click="Atualizar_Click" Foreground="Black"/>
        </Grid>
        </materialDesign:TransitioningContent>

        <Grid x:Name="GridCompras" Width="580" Height="300">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="20" Color="#FFDEDEDE" ShadowDepth="1"/>
            </Grid.Effect>
            <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect SlideInFromLeft, Duration=0:0:0.8}" Grid.ColumnSpan="2">                
                <Border BorderBrush="Black" BorderThickness="2" Background="FloralWhite">
                    <Label x:Name="lblError" Content="Não há compras no carrinho" HorizontalAlignment="Center" Width="160" Height="30" Margin="209,134,211,136" Foreground="Red"/>
                </Border>
            </materialDesign:TransitioningContent>
        </Grid>
        <Grid x:Name="GridListCompras" Width="580" Height="300" Visibility="Hidden">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="20" Color="#FFDEDEDE" ShadowDepth="1"/>
            </Grid.Effect>
            <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect FadeIn, Duration=0:0:0.8}" Grid.ColumnSpan="2">
                <Border BorderBrush="Black" BorderThickness="2" Background="FloralWhite">

                    <ListView Width="580" Height="300">
                        <ListViewItem Content="Passagem Gol"></ListViewItem>
                        <ListViewItem Content="Hotel ***"></ListViewItem>
                    </ListView>
                    
                </Border>
            </materialDesign:TransitioningContent>
        </Grid>

        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect SlideInFromLeft, Duration=0:0:0.8}" Grid.ColumnSpan="2">
            <Grid x:Name="GridInferior" Width="580" Height="65" Margin="14,372,14,7">            
                <TextBox Width="300" Height="50" materialDesign:HintAssist.Hint="Número do Cartão"  Background="FloralWhite" Margin="0,5,280,10"></TextBox>
                <Button Width="200" Height="50" Background="LightSeaGreen" Content="Finalizar Compra" Margin="380,5,0,10"></Button>            
            </Grid>
        </materialDesign:TransitioningContent>
        
    </Grid>
</UserControl>
