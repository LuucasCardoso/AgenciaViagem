<UserControl x:Class="ViewWPF.Views.Administrador.UsuarioListar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewWPF.Views.Administrador"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Height="auto" Width="auto">
  
    <Grid x:Name="GridListarUsuario" Margin="10" VerticalAlignment="Top">
            <DataGrid x:Name="dgUsuarios" CanUserAddRows="False" ItemsSource="{Binding Usuarios}" CanUserResizeColumns="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" IsReadOnly="True" Binding="{Binding Nome}"></DataGridTextColumn>
                    <DataGridTextColumn Header="E-mail" Width="360" IsReadOnly="True" Binding="{Binding Email}"></DataGridTextColumn>
                <DataGridTextColumn Header="CPF" Width="360" IsReadOnly="True" Binding="{Binding CPF}"></DataGridTextColumn>
            </DataGrid.Columns>
            </DataGrid>
        </Grid>
</UserControl>
